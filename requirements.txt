# Core
datasets>=2.18
transformers>=4.44
torch>=2.2
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.8

# GPU fast path (install only where a CUDA GPU is present; skip locally on CPU/mac)
# vllm>=0.5

# Remote runner
kaggle>=1.6
